# パスワードマネージャー（学習用）

ブラウザで動くシンプルなパスワードマネージャーのデモです。

使い方:

1. このフォルダ内の `index.html` をブラウザで開いてください（Microsoft Edge 推奨）。
2. 初回はマスターパスワードを設定します。次回以降はそのパスワードでログインしてください。
3. サービスの登録、検索、編集、削除が可能です。データは LocalStorage に保存されます。

注意:

- 学習用の簡易実装です。本番での利用は推奨しません（LocalStorage やブラウザ内保管の安全性は限定的です）。
